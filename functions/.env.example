# Copie para .env.projetoafiliado-9ff07 (ou deixe o arquivo do projeto no repo).
# O deploy do Firebase aplica estas variaveis — sem abrir Google Cloud Console.
#
# promosapp = batimento mestredolink.promos.app (node_once)
# api       = api-faithful-v2 (Insights Shopee / soma por linha)
SHOPEE_AGG_MODE=promosapp
